import { PersianConverterPipe } from './persian-converter.pipe';

describe('PersianConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new PersianConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
